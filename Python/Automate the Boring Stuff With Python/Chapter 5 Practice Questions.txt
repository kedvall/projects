1.	newDict = {}
2.	newDict = {'foo': 42}
3.	A list is just a set of ordered values, a dictionary has unique keys and values
4.	It will return a KeyError
5.	There is no difference. If none specified, it searches keys and not values
6.	'cat' in spam seaches for keys containing 'cat'. 'cat' in spam.values() looks for cat only in values of the dictionary
7.	spam.setdefault('color', 'black')
8.	The pretty print module. Specifically pprint.pprint or pprint.pformat